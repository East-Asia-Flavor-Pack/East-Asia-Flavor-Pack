# 일본

INJECT:STATE_CHUBU = {
    resource = { # 추가
        type = "bg_gold_fields"
        depleted_type = "bg_gold_mining"
        undiscovered_amount = 1
    }
}

INJECT:STATE_CHUGOKU = {
    resource = { # 추가
        type = "bg_gold_fields"
        depleted_type = "bg_gold_mining"
        undiscovered_amount = 1
    }
}
