INJECT:send_to_jail = { # 수정
	possible = {
		trigger_if = { # 추가, 비밀리에 활동중...모디파이어(eafp_modi_missionary_stealth)가 없어야 함
			limit = { c:KOR ?= scope:actor }
			NOT = { has_modifier = eafp_modi_missionary_stealth }
		}
	}
}