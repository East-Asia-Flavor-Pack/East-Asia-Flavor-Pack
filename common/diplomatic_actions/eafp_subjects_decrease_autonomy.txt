INJECT:da_decrease_autonomy = {
	ai = {
		evaluation_chance = {
			if = {
				limit = {
					AND = { # 추가
						is_power_bloc_leader = yes
						power_bloc = { has_identity = identity:identity_eafp_mandate_of_heaven }
						#scope:target_country = {
						#	is_in_same_power_bloc = ROOT
						#	is_subject_type = subject_type_tributary
						#	is_subject_of = ROOT
						#}
					}
				}
				value = 0
			}
		}
	}
}

