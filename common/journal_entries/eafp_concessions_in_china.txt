je_concessions_in_china = { # 중국
	icon = "gfx/interface/icons/event_icons/event_scales.dds"
	
	immediate = {
		s:STATE_GUIZHOU = { save_scope_as = STATE_GUIZHOU_scope }
		s:STATE_CHONGQING = { save_scope_as = STATE_CHONGQING_scope }
		s:STATE_SICHUAN = { save_scope_as = STATE_SICHUAN_scope }
		s:STATE_SOUTHERN_ANHUI = { save_scope_as = STATE_SOUTHERN_ANHUI_scope }
		s:STATE_SHAOZHOU = { save_scope_as = STATE_SHAOZHOU_scope }
		s:STATE_GUANGDONG = { save_scope_as = STATE_GUANGDONG_scope }
		s:STATE_FUJIAN = { save_scope_as = STATE_FUJIAN_scope }
		s:STATE_FORMOSA = { save_scope_as = STATE_FORMOSA_scope }
		s:STATE_GUANGXI = { save_scope_as = STATE_GUANGXI_scope }
		s:STATE_ZHEJIANG = { save_scope_as = STATE_ZHEJIANG_scope }
		s:STATE_SUZHOU = { save_scope_as = STATE_SUZHOU_scope }
		s:STATE_NANJING = { save_scope_as = STATE_NANJING_scope }
		s:STATE_HUNAN = { save_scope_as = STATE_HUNAN_scope }
		s:STATE_JIANGXI = { save_scope_as = STATE_JIANGXI_scope }
		s:STATE_YUNNAN = { save_scope_as = STATE_YUNNAN_scope }
		s:STATE_EASTERN_HUBEI = { save_scope_as = STATE_EASTERN_HUBEI_scope }
		s:STATE_WESTERN_HUBEI = { save_scope_as = STATE_WESTERN_HUBEI_scope }
		s:STATE_SHANDONG = { save_scope_as = STATE_SHANDONG_scope }
		s:STATE_BEIJING = { save_scope_as = STATE_BEIJING_scope }
		s:STATE_ZHILI = { save_scope_as = STATE_ZHILI_scope }
		s:STATE_HENAN = { save_scope_as = STATE_HENAN_scope }
		s:STATE_NORTHERN_ANHUI = { save_scope_as = STATE_NORTHERN_ANHUI_scope }
		s:STATE_SHANXI = { save_scope_as = STATE_SHANXI_scope }
		s:STATE_JIANGSU = { save_scope_as = STATE_JIANGSU_scope }
		s:STATE_NINGXIA = { save_scope_as = STATE_NINGXIA_scope }
		s:STATE_GANSU = { save_scope_as = STATE_GANSU_scope }
		s:STATE_XIAN = { save_scope_as = STATE_XIAN_scope }
		if = {
			limit = {
				s:STATE_GUIZHOU = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_GUIZHOU_has_concession
		}
		else = { remove_variable = STATE_GUIZHOU_has_concession }
		if = {
			limit = {
				s:STATE_CHONGQING = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_CHONGQING_has_concession
		}
		else = { remove_variable = STATE_CHONGQING_has_concession }
		if = {
			limit = {
				s:STATE_SICHUAN = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_SICHUAN_has_concession
		}
		else = { remove_variable = STATE_SICHUAN_has_concession }
		if = {
			limit = {
				s:STATE_SOUTHERN_ANHUI = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_SOUTHERN_ANHUI_has_concession
		}
		else = { remove_variable = STATE_SOUTHERN_ANHUI_has_concession }
		if = {
			limit = {
				s:STATE_SHAOZHOU = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_SHAOZHOU_has_concession
		}
		else = { remove_variable = STATE_SHAOZHOU_has_concession }
		if = {
			limit = {
				s:STATE_GUANGDONG = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_GUANGDONG_has_concession
		}
		else = { remove_variable = STATE_GUANGDONG_has_concession }
		if = {
			limit = {
				s:STATE_FUJIAN = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_FUJIAN_has_concession
		}
		else = { remove_variable = STATE_FUJIAN_has_concession }
		if = {
			limit = {
				s:STATE_FORMOSA = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_FORMOSA_has_concession
		}
		else = { remove_variable = STATE_FORMOSA_has_concession }
		if = {
			limit = {
				s:STATE_GUANGXI = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_GUANGXI_has_concession
		}
		else = { remove_variable = STATE_GUANGXI_has_concession }
		if = {
			limit = {
				s:STATE_ZHEJIANG = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_ZHEJIANG_has_concession
		}
		else = { remove_variable = STATE_ZHEJIANG_has_concession }
		if = {
			limit = {
				s:STATE_SUZHOU = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_SUZHOU_has_concession
		}
		else = { remove_variable = STATE_SUZHOU_has_concession }
		if = {
			limit = {
				s:STATE_NANJING = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_NANJING_has_concession
		}
		else = { remove_variable = STATE_NANJING_has_concession }
		if = {
			limit = {
				s:STATE_HUNAN = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_HUNAN_has_concession
		}
		else = { remove_variable = STATE_HUNAN_has_concession }
		if = {
			limit = {
				s:STATE_JIANGXI = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_JIANGXI_has_concession
		}
		else = { remove_variable = STATE_JIANGXI_has_concession }
		if = {
			limit = {
				s:STATE_YUNNAN = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_YUNNAN_has_concession
		}
		else = { remove_variable = STATE_YUNNAN_has_concession }
		if = {
			limit = {
				s:STATE_EASTERN_HUBEI = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_EASTERN_HUBEI_has_concession
		}
		else = { remove_variable = STATE_EASTERN_HUBEI_has_concession }
		if = {
			limit = {
				s:STATE_WESTERN_HUBEI = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_WESTERN_HUBEI_has_concession
		}
		else = { remove_variable = STATE_WESTERN_HUBEI_has_concession }
		if = {
			limit = {
				s:STATE_SHANDONG = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_SHANDONG_has_concession
		}
		else = { remove_variable = STATE_SHANDONG_has_concession }
		if = {
			limit = {
				s:STATE_BEIJING = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_BEIJING_has_concession
		}
		else = { remove_variable = STATE_BEIJING_has_concession }
		if = {
			limit = {
				s:STATE_ZHILI = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_ZHILI_has_concession
		}
		else = { remove_variable = STATE_ZHILI_has_concession }
		if = {
			limit = {
				s:STATE_HENAN = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_HENAN_has_concession
		}
		else = { remove_variable = STATE_HENAN_has_concession }
		if = {
			limit = {
				s:STATE_NORTHERN_ANHUI = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_NORTHERN_ANHUI_has_concession
		}
		else = { remove_variable = STATE_NORTHERN_ANHUI_has_concession }
		if = {
			limit = {
				s:STATE_SHANXI = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_SHANXI_has_concession
		}
		else = { remove_variable = STATE_SHANXI_has_concession }
		if = {
			limit = {
				s:STATE_JIANGSU = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_JIANGSU_has_concession
		}
		else = { remove_variable = STATE_JIANGSU_has_concession }
		if = {
			limit = {
				s:STATE_NINGXIA = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_NINGXIA_has_concession
		}
		else = { remove_variable = STATE_NINGXIA_has_concession }
		if = {
			limit = {
				s:STATE_GANSU = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_GANSU_has_concession
		}
		else = { remove_variable = STATE_GANSU_has_concession }
		if = {
			limit = {
				s:STATE_XIAN = {
					any_state = {
						has_variable_list = concession_owner_varlist
						owner = ROOT
					}
				}
			}
			set_variable = STATE_XIAN_has_concession
		}
		else = { remove_variable = STATE_XIAN_has_concession }

	}

	complete = {
		# NOT = { any_scope_state = { has_variable_list = concession_owner_varlist } }
		always = no
	}

	on_complete = {
		hidden_effect = {
			if = {
				limit = { has_variable = STATE_GUIZHOU_has_concession }
				remove_variable = STATE_GUIZHOU_has_concession
			}
			if = {
				limit = { has_variable = STATE_CHONGQING_has_concession }
				remove_variable = STATE_CHONGQING_has_concession
			}
			if = {
				limit = { has_variable = STATE_SICHUAN_has_concession }
				remove_variable = STATE_SICHUAN_has_concession
			}
			if = {
				limit = { has_variable = STATE_SOUTHERN_ANHUI_has_concession }
				remove_variable = STATE_SOUTHERN_ANHUI_has_concession
			}
			if = {
				limit = { has_variable = STATE_SHAOZHOU_has_concession }
				remove_variable = STATE_SHAOZHOU_has_concession
			}
			if = {
				limit = { has_variable = STATE_GUANGDONG_has_concession }
				remove_variable = STATE_GUANGDONG_has_concession
			}
			if = {
				limit = { has_variable = STATE_FUJIAN_has_concession }
				remove_variable = STATE_FUJIAN_has_concession
			}
			if = {
				limit = { has_variable = STATE_FORMOSA_has_concession }
				remove_variable = STATE_FORMOSA_has_concession
			}
			if = {
				limit = { has_variable = STATE_GUANGXI_has_concession }
				remove_variable = STATE_GUANGXI_has_concession
			}
			if = {
				limit = { has_variable = STATE_ZHEJIANG_has_concession }
				remove_variable = STATE_ZHEJIANG_has_concession
			}
			if = {
				limit = { has_variable = STATE_SUZHOU_has_concession }
				remove_variable = STATE_SUZHOU_has_concession
			}
			if = {
				limit = { has_variable = STATE_NANJING_has_concession }
				remove_variable = STATE_NANJING_has_concession
			}
			if = {
				limit = { has_variable = STATE_HUNAN_has_concession }
				remove_variable = STATE_HUNAN_has_concession
			}
			if = {
				limit = { has_variable = STATE_JIANGXI_has_concession }
				remove_variable = STATE_JIANGXI_has_concession
			}
			if = {
				limit = { has_variable = STATE_YUNNAN_has_concession }
				remove_variable = STATE_YUNNAN_has_concession
			}
			if = {
				limit = { has_variable = STATE_EASTERN_HUBEI_has_concession }
				remove_variable = STATE_EASTERN_HUBEI_has_concession
			}
			if = {
				limit = { has_variable = STATE_WESTERN_HUBEI_has_concession }
				remove_variable = STATE_WESTERN_HUBEI_has_concession
			}
			if = {
				limit = { has_variable = STATE_SHANDONG_has_concession }
				remove_variable = STATE_SHANDONG_has_concession
			}
			if = {
				limit = { has_variable = STATE_BEIJING_has_concession }
				remove_variable = STATE_BEIJING_has_concession
			}
			if = {
				limit = { has_variable = STATE_ZHILI_has_concession }
				remove_variable = STATE_ZHILI_has_concession
			}
			if = {
				limit = { has_variable = STATE_HENAN_has_concession }
				remove_variable = STATE_HENAN_has_concession
			}
			if = {
				limit = { has_variable = STATE_NORTHERN_ANHUI_has_concession }
				remove_variable = STATE_NORTHERN_ANHUI_has_concession
			}
			if = {
				limit = { has_variable = STATE_SHANXI_has_concession }
				remove_variable = STATE_SHANXI_has_concession
			}
			if = {
				limit = { has_variable = STATE_JIANGSU_has_concession }
				remove_variable = STATE_JIANGSU_has_concession
			}
			if = {
				limit = { has_variable = STATE_NINGXIA_has_concession }
				remove_variable = STATE_NINGXIA_has_concession
			}
			if = {
				limit = { has_variable = STATE_GANSU_has_concession }
				remove_variable = STATE_GANSU_has_concession
			}
			if = {
				limit = { has_variable = STATE_XIAN_has_concession }
				remove_variable = STATE_XIAN_has_concession
			}
		}
	}

	on_monthly_pulse = {
		concession_state_status_calc = yes
	}
	
	weight = 600

	transferable = yew
}

je_concessions_in_china2 = { # 상대
	icon = "gfx/interface/icons/event_icons/event_scales.dds"
	
	immediate = {
		
	}

	complete = {
		# NOT = { any_state = { is_target_in_variable_list = { name = concession_owner_varlist target = ROOT } } }
		always = no
	}

	on_complete = {
		
	}

	on_monthly_pulse = {

	}
	
	weight = 600

	transferable = yew
}