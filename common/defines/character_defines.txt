NCharacters = {
	MAX_NUM_CHARACTERS_PER_TYPE = 1000		# The maximum number of characters of a certain primary role (e.g. General, Admiral) you can manually recruit. This is a hard cap failsafe and should be set high enough that players have no legitimate reason to recruit more than this.
}
