amendment_five_household_mutual_responsibility_system= {

	parent = law_secret_police

	allowed_laws = {
		law_no_home_affairs
	}

	modifier = {
		country_suppression_attraction_factor = -0.05
		country_coup_resistance_add = 0.5
	}

	possible = {
		country_has_primary_culture = cu:korean
	}

	would_sponsor = {
		AND = {
			exists = scope:approval
			scope:approval > 0
		}
	}

	ai_will_revoke = {
		always = no
	}

	can_repeal = {
		legitimacy >= legitimacy_to_repeal_amendment
	}
}

amendment_nobi_system= {

	parent = law_serfdom

	allowed_laws = {
		law_serfdom
	}

	modifier = {
		interest_group_ig_landowners_pol_str_mult = 0.05
		state_minimum_incorporated_subsistence_arable_land_add = -0.50
		building_minimum_incorporated_subsistence_employment_add = -0.25
	}

	possible = {
		country_has_primary_culture = cu:korean
	}

	would_sponsor = {
		AND = {
			exists = scope:approval
			scope:approval > 0
		}
	}

	ai_will_revoke = {
		always = no
	}

	can_repeal = {
		legitimacy >= legitimacy_to_repeal_amendment
	}
}

amendment_seowon= {

	parent = law_state_religion

	allowed_laws = {
		law_no_schools
	}

	modifier = {
		state_conversion_mult = 0.2
		state_assimilation_mult = 0.2
		state_education_access_add = 0.05
		state_education_access_wealth_add = 0.0025
		state_tax_waste_add = 0.1
		country_clergymen_pol_str_mult = 0.25
		state_pop_support_movement_royalist_absolutist_mult = 0.2
		state_radicals_from_political_movements_mult = -0.2
	}

	possible = {
		country_has_primary_culture = cu:korean
	}

	would_sponsor = {
		AND = {
			exists = scope:approval
			scope:approval > 0
		}
	}

	ai_will_revoke = {
		NOT = { has_law_or_variant = law_type:law_state_religion }
		NOT = { has_law_or_variant = law_type:law_freedom_of_conscience }
	}

	can_repeal = {
		OR = {
			AND = {
				NOT = { has_law_or_variant = law_type:law_state_religion }
				NOT = { has_law_or_variant = law_type:law_freedom_of_conscience }
			}
			legitimacy >= legitimacy_to_repeal_amendment
		}
	}
}

amendment_licensed_merchants= {

	parent = law_autocracy

	allowed_laws = {
		law_guild_system
	}

	modifier = {
		country_bureaucrats_pol_str_mult = 0.25
		building_self_investment_chance_add = 0.25

		state_pop_qualifications_mult = -0.2
	}

	possible = {
		country_has_primary_culture = cu:korean
	}

	would_sponsor = {
		AND = {
			exists = scope:approval
			scope:approval > 0
		}
	}

	ai_will_revoke = {
		always = no
	}

	can_repeal = {
		legitimacy >= legitimacy_to_repeal_amendment
	}
}

amendment_podocheong= {

	parent = law_local_police

	allowed_laws = {
		law_no_police
	}

	modifier = {
		interest_group_ig_landowners_pol_str_mult = 0.05
		state_turmoil_effects_mult = -0.05
		country_coup_resistance_add = 0.5
	}

	possible = {
		country_has_primary_culture = cu:korean
	}

	would_sponsor = {
		AND = {
			exists = scope:approval
			scope:approval > 0
		}
	}

	ai_will_revoke = {
		always = no
	}

	can_repeal = {
		legitimacy >= legitimacy_to_repeal_amendment
	}
}



