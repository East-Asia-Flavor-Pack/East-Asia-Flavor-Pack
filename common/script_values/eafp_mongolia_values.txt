eafp_mgl_heartland_state_count = {
	value = 0
	every_scope_state = {
		limit = { is_eafp_mgl_heartland_state = yes }
		add = 1
	}
}

eafp_mgl_heartland_construction_levels = {
	value = 0
	every_scope_building = {
		limit = { state = { is_eafp_mgl_heartland_state = yes } is_building_type = building_construction_sector }
		add = this.level
	}
}

eafp_mgl_heartland_coal_levels = {
	value = 0
	every_scope_building = {
		limit = { state = { is_eafp_mgl_heartland_state = yes } is_building_type = building_coal_mine }
		add = this.level
	}
}

eafp_mgl_heartland_iron_levels = {
	value = 0
	every_scope_building = {
		limit = { state = { is_eafp_mgl_heartland_state = yes } is_building_type = building_iron_mine }
		add = this.level
	}
}

eafp_mgl_heartland_logging_levels = {
	value = 0
	every_scope_building = {
		limit = { state = { is_eafp_mgl_heartland_state = yes } is_building_type = building_logging_camp }
		add = this.level
	}
}

eafp_mgl_heartland_raw_material_levels = {
	value = eafp_mgl_heartland_coal_levels
	add = eafp_mgl_heartland_iron_levels
	add = eafp_mgl_heartland_logging_levels
}

eafp_mgl_heartland_raw_material_types = {
	value = 0
	if = { limit = { eafp_mgl_heartland_coal_levels >= 1 } add = 1 }
	if = { limit = { eafp_mgl_heartland_iron_levels >= 1 } add = 1 }
	if = { limit = { eafp_mgl_heartland_logging_levels >= 1 } add = 1 }
}

eafp_mgl_heartland_administration_levels = {
	value = 0
	every_scope_building = {
		limit = { state = { is_eafp_mgl_heartland_state = yes } is_building_type = building_government_administration }
		add = this.level
	}
}

eafp_mgl_heartland_urban_levels = {
	value = 0
	every_scope_building = {
		limit = { state = { is_eafp_mgl_heartland_state = yes } is_building_type = building_urban_center }
		add = this.level
	}
}

eafp_mgl_heartland_tools_power_levels = {
	value = 0
	every_scope_building = {
		limit = {
			state = { is_eafp_mgl_heartland_state = yes }
			OR = { is_building_type = building_tooling_workshop is_building_type = building_power_plant }
		}
		add = this.level
	}
}

eafp_mgl_heartland_light_industry_levels = {
	value = 0
	every_scope_building = {
		limit = {
			state = { is_eafp_mgl_heartland_state = yes }
			OR = { is_building_type = building_food_industry is_building_type = building_textile_mill }
		}
		add = this.level
	}
}

eafp_mgl_united_state_count = {
	value = 0
	every_country = {
		limit = { is_eafp_mgl_united_industry_partner = yes }
		every_scope_state = {
			limit = { is_eafp_mgl_united_industrial_state = yes }
			add = 1
		}
	}
}

eafp_mgl_united_stable_state_count = {
	value = 0
	every_country = {
		limit = { is_eafp_mgl_united_industry_partner = yes }
		every_scope_state = {
			limit = { is_eafp_mgl_united_industrial_state = yes market_access >= 0.85 devastation < 0.1 turmoil < 0.15 }
			add = 1
		}
	}
}

eafp_mgl_united_sustainable_state_count = {
	value = 0
	every_country = {
		limit = { is_eafp_mgl_united_industry_partner = yes }
		every_scope_state = {
			limit = { is_eafp_mgl_united_industrial_state = yes market_access >= 0.9 average_sol >= 10 devastation < 0.1 turmoil < 0.15 }
			add = 1
		}
	}
}

eafp_mgl_united_admin_state_count = {
	value = 0
	every_country = {
		limit = { OR = { c:MGL ?= this top_overlord = c:MGL } }
		every_scope_state = {
			limit = { is_eafp_mgl_united_industrial_state = yes }
			add = 1
		}
	}
}

eafp_mgl_united_admin_ready_state_count = {
	value = 0
	every_country = {
		limit = { OR = { c:MGL ?= this top_overlord = c:MGL } }
		every_scope_state = {
			limit = { is_eafp_mgl_united_industrial_state = yes tax_capacity >= tax_capacity_usage }
			add = 1
		}
	}
}

eafp_mgl_united_coal_levels = {
	value = 0
	every_country = { limit = { is_eafp_mgl_united_industry_partner = yes } every_scope_building = { limit = { state = { is_eafp_mgl_united_industrial_state = yes } is_building_type = building_coal_mine } add = this.level } }
}

eafp_mgl_united_iron_levels = {
	value = 0
	every_country = { limit = { is_eafp_mgl_united_industry_partner = yes } every_scope_building = { limit = { state = { is_eafp_mgl_united_industrial_state = yes } is_building_type = building_iron_mine } add = this.level } }
}

eafp_mgl_united_lead_levels = {
	value = 0
	every_country = { limit = { is_eafp_mgl_united_industry_partner = yes } every_scope_building = { limit = { state = { is_eafp_mgl_united_industrial_state = yes } is_building_type = building_lead_mine } add = this.level } }
}

eafp_mgl_united_sulfur_levels = {
	value = 0
	every_country = { limit = { is_eafp_mgl_united_industry_partner = yes } every_scope_building = { limit = { state = { is_eafp_mgl_united_industrial_state = yes } is_building_type = building_sulfur_mine } add = this.level } }
}

eafp_mgl_united_logging_levels = {
	value = 0
	every_country = { limit = { is_eafp_mgl_united_industry_partner = yes } every_scope_building = { limit = { state = { is_eafp_mgl_united_industrial_state = yes } is_building_type = building_logging_camp } add = this.level } }
}

eafp_mgl_united_raw_material_levels = {
	value = eafp_mgl_united_coal_levels
	add = eafp_mgl_united_iron_levels
	add = eafp_mgl_united_lead_levels
	add = eafp_mgl_united_sulfur_levels
	add = eafp_mgl_united_logging_levels
}

eafp_mgl_united_raw_material_types = {
	value = 0
	if = { limit = { eafp_mgl_united_coal_levels >= 1 } add = 1 }
	if = { limit = { eafp_mgl_united_iron_levels >= 1 } add = 1 }
	if = { limit = { eafp_mgl_united_lead_levels >= 1 } add = 1 }
	if = { limit = { eafp_mgl_united_sulfur_levels >= 1 } add = 1 }
	if = { limit = { eafp_mgl_united_logging_levels >= 1 } add = 1 }
}

eafp_mgl_united_transport_power_levels = {
	value = 0
	every_country = {
		limit = { is_eafp_mgl_united_industry_partner = yes }
		every_scope_building = {
			limit = { state = { is_eafp_mgl_united_industrial_state = yes } OR = { is_building_type = building_construction_sector is_building_type = building_railway is_building_type = building_power_plant } }
			add = this.level
		}
	}
}

eafp_mgl_united_manufacturing_levels = {
	value = 0
	every_country = {
		limit = { is_eafp_mgl_united_industry_partner = yes }
		every_scope_building = {
			limit = { state = { is_eafp_mgl_united_industrial_state = yes } OR = { is_building_type = building_tooling_workshop is_building_type = building_food_industry is_building_type = building_textile_mill } }
			add = this.level
		}
	}
}

eafp_mgl_united_manufacturing_state_count = {
	value = 0
	every_country = {
		limit = { is_eafp_mgl_united_industry_partner = yes }
		every_scope_state = {
			limit = { is_eafp_mgl_united_industrial_state = yes any_scope_building = { OR = { is_building_type = building_tooling_workshop is_building_type = building_food_industry is_building_type = building_textile_mill } level >= 1 } }
			add = 1
		}
	}
}

eafp_mgl_united_stable_state_share = {
	value = eafp_mgl_united_stable_state_count
	if = { limit = { eafp_mgl_united_state_count > 0 } divide = eafp_mgl_united_state_count }
}

eafp_mgl_united_sustainable_state_share = {
	value = eafp_mgl_united_sustainable_state_count
	if = { limit = { eafp_mgl_united_state_count > 0 } divide = eafp_mgl_united_state_count }
}

eafp_mgl_united_admin_ready_state_share = {
	value = eafp_mgl_united_admin_ready_state_count
	if = { limit = { eafp_mgl_united_admin_state_count > 0 } divide = eafp_mgl_united_admin_state_count }
}

eafp_mgl_united_raw_building_count = {
	value = 0
	every_country = {
		limit = { is_eafp_mgl_united_industry_partner = yes }
		every_scope_building = {
			limit = { state = { is_eafp_mgl_united_industrial_state = yes } OR = { is_building_type = building_coal_mine is_building_type = building_iron_mine is_building_type = building_lead_mine is_building_type = building_sulfur_mine is_building_type = building_logging_camp } }
			add = 1
		}
	}
}

eafp_mgl_united_healthy_raw_building_count = {
	value = 0
	every_country = {
		limit = { is_eafp_mgl_united_industry_partner = yes }
		every_scope_building = {
			limit = { state = { is_eafp_mgl_united_industrial_state = yes } OR = { is_building_type = building_coal_mine is_building_type = building_iron_mine is_building_type = building_lead_mine is_building_type = building_sulfur_mine is_building_type = building_logging_camp } cash_reserves_ratio > 0.25 }
			add = 1
		}
	}
}

eafp_mgl_united_healthy_raw_building_share = {
	value = eafp_mgl_united_healthy_raw_building_count
	if = { limit = { eafp_mgl_united_raw_building_count > 0 } divide = eafp_mgl_united_raw_building_count }
}

eafp_mgl_united_industry_building_count = {
	value = 0
	every_country = {
		limit = { is_eafp_mgl_united_industry_partner = yes }
		every_scope_building = {
			limit = { state = { is_eafp_mgl_united_industrial_state = yes } OR = { is_building_type = building_tooling_workshop is_building_type = building_power_plant is_building_type = building_food_industry is_building_type = building_textile_mill } }
			add = 1
		}
	}
}

eafp_mgl_united_healthy_industry_building_count = {
	value = 0
	every_country = {
		limit = { is_eafp_mgl_united_industry_partner = yes }
		every_scope_building = {
			limit = { state = { is_eafp_mgl_united_industrial_state = yes } OR = { is_building_type = building_tooling_workshop is_building_type = building_power_plant is_building_type = building_food_industry is_building_type = building_textile_mill } cash_reserves_ratio > 0.25 }
			add = 1
		}
	}
}

eafp_mgl_united_healthy_industry_building_share = {
	value = eafp_mgl_united_healthy_industry_building_count
	if = { limit = { eafp_mgl_united_industry_building_count > 0 } divide = eafp_mgl_united_industry_building_count }
}
