INJECT:grant_command_to_agitator = { # 수정
	effect = {
		if = { # 추가, 비밀선교사가 아니거나 완전분리 제정
			limit = {
				c:KOR ?= scope:actor
				OR = {
					NOT = { has_ideology = ideology:ideology_eafp_missionary }
					owner = {
						has_law = law_type:law_total_separation
					}
				}
			}
			remove_character_role = general
		}
	}
	ai_chance = {
		if = {
			limit = {
				c:KOR ?= scope:actor
				OR = {
					NOT = { has_ideology = ideology:ideology_eafp_missionary }
					owner = {
						has_law = law_type:law_total_separation
					}
				}
			}
			value = 0
		}
	}
}