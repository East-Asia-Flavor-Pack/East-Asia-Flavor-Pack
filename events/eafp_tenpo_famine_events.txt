namespace = tenpo_famine

tenpo_famine.1 = { # 텐포 대기근의 절정
	type = country_event
	placement = root

	title = tenpo_famine.1.t
	desc = tenpo_famine.1.d
	flavor = tenpo_famine.1.f

	icon = "gfx/interface/icons/event_icons/event_skull.dds"

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	event_image = { 
		video = "asia_confucianism_shinto"
	}

	duration = 3
	
	immediate = {
		add_journal_entry = {
			type = je_tenpo_famine
		}
	}

	trigger = {}
	
	option = { # 해결 노력
		name = tenpo_famine.1.a
		default_option = yes
		show_as_tooltip = {
			add_journal_entry = {
				type = je_tenpo_famine
			}
		}
		ai_chance = {
			base = 50
		}
	}
}

tenpo_famine.2 = { # 니도메
	type = country_event
	placement = root

	title = tenpo_famine.2.t
	desc = tenpo_famine.2.d
	flavor = tenpo_famine.2.f

	icon = "gfx/interface/icons/event_icons/event_scales.dds"

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	event_image = { 
		video = "asia_confucianism_shinto"
	}

	duration = 3
	
	immediate = {
		every_scope_state = {
			limit = {
				OR = {
					region = s:STATE_TOHOKU
					region = s:STATE_KANTO
					region = s:STATE_CHUBU
					region = s:STATE_KANSAI
					region = s:STATE_KYUSHU
					region = s:STATE_CHUGOKU
					region = s:STATE_SHIKOKU
				}
			}
			add_modifier = {
				name = rice_export_ban_state_modifier
			}
		}
	}

	trigger = {}
	
	option = { # 버튼 해금
		name = tenpo_famine.2.a
		default_option = yes

		show_as_tooltip = {
			every_scope_state = {
				limit = {
					OR = {
						region = s:STATE_TOHOKU
						region = s:STATE_KANTO
						region = s:STATE_CHUBU
						region = s:STATE_KANSAI
						region = s:STATE_KYUSHU
						region = s:STATE_CHUGOKU
						region = s:STATE_SHIKOKU
					}
				}
				add_modifier = {
					name = rice_export_ban_state_modifier
				}
			}
		}
		custom_tooltip = tenpo_famine.2.ta

		ai_chance = {
			base = 50
		}
	}
}