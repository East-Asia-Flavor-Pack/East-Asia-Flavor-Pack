ai_strategy_eafp_mgl_industrialization = {
	icon = "gfx/interface/icons/ai_strategy_icons/industrial_expansion.dds"
	type = administrative

	subsidies = {
		building_construction_sector = nice_to_have
		building_coal_mine = nice_to_have
		building_iron_mine = nice_to_have
		building_logging_camp = nice_to_have
		building_tooling_workshop = nice_to_have
		building_power_plant = nice_to_have
		building_food_industry = nice_to_have
		building_textile_mill = nice_to_have
	}

	goods_stances = {
		wood = { stance = wants_high_supply }
		iron = { stance = wants_high_supply }
		coal = { stance = wants_high_supply }
		tools = { stance = wants_high_supply }
		fabric = { stance = wants_high_supply }
		groceries = { stance = wants_high_supply }
		clothes = { stance = wants_high_supply }
		electricity = { stance = wants_high_supply trigger = { has_technology_researched = electrical_generation } }
	}

	building_group_weights = {
		bg_construction = 1.25
		bg_mining = 1.25
		bg_logging = 1.15
		bg_manufacturing = 1.20
		bg_light_industry = 1.20
		bg_infrastructure = 1.15
	}

	possible = { c:MGL ?= THIS }

	economic_vs_government_spending_balance = {
		if = { limit = { is_at_war = no } add = 0.10 }
		if = { limit = { any_scope_state = { is_eafp_mgl_heartland_state = yes market_access < 0.85 } } add = 0.05 }
	}

	weight = {
		value = 10
		if = { limit = { has_journal_entry = je_eafp_mgl_develop_the_heartland } add = 1000 }
		if = { limit = { has_journal_entry = je_eafp_mgl_develop_the_united_mongol_lands } add = 1000 }
	}
}
