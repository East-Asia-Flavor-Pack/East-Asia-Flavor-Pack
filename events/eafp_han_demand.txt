namespace = han_demand

han_demand.1 = { # 만주와 한 TODO
	type = country_event
	placement = ROOT

	title = han_demand.1.t
	desc = han_demand.1.d
	flavor = han_demand.1.f

	duration = 3

	event_image = {
		texture = "gfx/event_pictures/china/coronation.png"
	}

	trigger = {
		has_technology_progress = { technology = nationalism progress > 0 }
		country_has_primary_culture = cu:han
	}

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_newspaper.dds"

	immediate = {
		
	}

	option = {
		name = han_demand.1.a
		default_option = yes
		
		set_variable = manchus_and_han

		custom_tooltip = {
			text = warlord_china_explosion_point_add_10_tt
			post_notification = warlord_china_explosion_point_add
			change_variable = {
				name = china_warlord_explosion
				add = 0.5
			}
		}
		
		change_variable = {
			name = chi_non_centralized_minting_var
			subtract = 1
		}
		remove_modifier = chi_non_centralized_minting
		if = {
			limit = { var:chi_non_centralized_minting_var = 1 }
			add_modifier = { name = chi_non_centralized_minting }
			trigger_event = chinese_westernization.403
		}
		else = {
			add_modifier = {
				name = chi_non_centralized_minting
				multiplier = var:chi_non_centralized_minting_var
			}
		}

		ai_chance = {
			base = 100
		}
	}
}